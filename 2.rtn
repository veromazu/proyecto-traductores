func incremental_forward(number steps, boolean x)
begin
    for i from 1 to steps do
        forward(i);
    end;
end;

program
    for i from 1 to 360 do
        incremental_forward(i);
        rotater(15);
    end;
end;
