func min(number x, number y) -> number
begin
    with
        boolean b;
    do
        b = x < y;
        if b then
            return x;
        end;
        return y;
    end;
end;
program
	min(35,e,4,t,2);
	with 
	number z;
	do z = 5;
	end;
end;
