func min(number x, boolean y) -> number
begin
    with
        boolean b;
    do
        b = x < y;
        if b then
            return x;
        end;
        return y;
    end;
end;




program
	min(5+5);
	with 
	number z;
	do z = 5;
	end;
end;
