func min(number x, number y) -> number
begin
    with
        boolean b;
    do
        b = x < y;
        if b then
            return x;
        end;
        return y;
    end;
end;
program 
with
	boolean x;
	do 
	 x = md(a);
	end;
end;
